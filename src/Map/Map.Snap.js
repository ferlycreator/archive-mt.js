Z.Map.include({
	/**
     * 截图
     * @param  {Object} options 截图设置
     * @member maptalks.Map
     * @expose
     */
    snap:function(options) {
        var extent = options['extent'],
            zoom = options['zoom'],
            layers = options['layers'];
    }
});
